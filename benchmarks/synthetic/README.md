# Synthetic adapter placeholder

C9 will implement a deterministic null/planted-signal plugin against the frozen `QuantTaskPlugin` interface. C7 uses synthetic JSON contracts only and makes no performance claim.

