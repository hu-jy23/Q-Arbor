# Configs

Versioned run profiles belong here after their `QuantResearchContract` has been frozen. C7 keeps the canonical synthetic draft under `tests/fixtures/contracts/`; C8–C12 may add smoke profiles that reference frozen snapshots by hash.

Never store credentials, raw capability tokens, hidden split locations, or final seeds in this directory.

