# HM1 futures adapter placeholder

Only schema, access contract, and non-secret hashes may be tracked. Restricted raw HM1 data must remain at its user-owned source path and must never be copied into Q-Arbor.

